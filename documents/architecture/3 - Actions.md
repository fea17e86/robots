# Actions

## Movement

`move(direction, speed)`

`forward(speed)`

`backward(speed)`

`left(speed)`

`right(speed)`

`turn(direction, speed)`

`stop()`

## Items

`activate(item)`

`deactivate(item)`

`throw(item, position)`

`grab(item)`

`drop(item)`
